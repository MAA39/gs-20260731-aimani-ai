import { check, foreignKey, pgTable, text, timestamp, unique, index } from 'drizzle-orm/pg-core';
import { sql } from 'drizzle-orm';
import { user } from './auth.js';

export const organization = pgTable('organization', { id: text('id').primaryKey(), name: text('name').notNull(), slug: text('slug').notNull(), createdAt: timestamp('created_at', { withTimezone: true }).notNull(), updatedAt: timestamp('updated_at', { withTimezone: true }).notNull() }, (t) => [unique('organization_slug_unique').on(t.slug)]);
export const membership = pgTable('membership', { id: text('id').primaryKey(), userId: text('user_id').notNull().references(() => user.id, { onDelete: 'cascade' }), organizationId: text('organization_id').notNull().references(() => organization.id, { onDelete: 'cascade' }), displayName: text('display_name').notNull(), title: text('title'), role: text('role').notNull(), status: text('status').notNull(), createdAt: timestamp('created_at', { withTimezone: true }).notNull(), updatedAt: timestamp('updated_at', { withTimezone: true }).notNull() }, (t) => [unique('membership_user_organization_unique').on(t.userId, t.organizationId), unique('membership_id_organization_unique').on(t.id, t.organizationId), check('membership_role_check', sql`${t.role} in ('owner', 'manager', 'member')`), check('membership_status_check', sql`${t.status} in ('active', 'invited', 'suspended')`)]);
export const relationship = pgTable('relationship', { id: text('id').primaryKey(), organizationId: text('organization_id').notNull().references(() => organization.id, { onDelete: 'cascade' }), sourceMembershipId: text('source_membership_id').notNull(), targetMembershipId: text('target_membership_id').notNull(), kind: text('kind').notNull(), createdAt: timestamp('created_at', { withTimezone: true }).notNull(), updatedAt: timestamp('updated_at', { withTimezone: true }).notNull() }, (t) => [foreignKey({ columns: [t.sourceMembershipId, t.organizationId], foreignColumns: [membership.id, membership.organizationId], name: 'relationship_source_membership_fk' }).onDelete('cascade'), foreignKey({ columns: [t.targetMembershipId, t.organizationId], foreignColumns: [membership.id, membership.organizationId], name: 'relationship_target_membership_fk' }).onDelete('cascade'), check('relationship_distinct_memberships_check', sql`${t.sourceMembershipId} <> ${t.targetMembershipId}`), unique('relationship_org_source_target_kind_unique').on(t.organizationId, t.sourceMembershipId, t.targetMembershipId, t.kind), index('relationship_organization_source_kind_idx').on(t.organizationId,t.sourceMembershipId,t.kind), index('relationship_organization_target_kind_idx').on(t.organizationId,t.targetMembershipId,t.kind)]);
