import { check, foreignKey, index, pgTable, text, timestamp } from 'drizzle-orm/pg-core';
import { sql } from 'drizzle-orm';
import { organization, membership } from './organization.js';
export const todo = pgTable('todo', { id:text('id').primaryKey(), organizationId:text('organization_id').notNull().references(()=>organization.id,{onDelete:'cascade'}), contextMembershipId:text('context_membership_id').notNull(), creatorMembershipId:text('creator_membership_id').notNull(), assigneeMembershipId:text('assignee_membership_id').notNull(), title:text('title').notNull(), description:text('description'), status:text('status').notNull(), createdAt:timestamp('created_at',{withTimezone:true}).notNull(), updatedAt:timestamp('updated_at',{withTimezone:true}).notNull() }, t=>[foreignKey({columns:[t.contextMembershipId,t.organizationId],foreignColumns:[membership.id,membership.organizationId],name:'todo_context_membership_fk'}).onDelete('cascade'),foreignKey({columns:[t.creatorMembershipId,t.organizationId],foreignColumns:[membership.id,membership.organizationId],name:'todo_creator_membership_fk'}).onDelete('cascade'),foreignKey({columns:[t.assigneeMembershipId,t.organizationId],foreignColumns:[membership.id,membership.organizationId],name:'todo_assignee_membership_fk'}).onDelete('cascade'),check('todo_context_creator_distinct_check',sql`${t.contextMembershipId} <> ${t.creatorMembershipId}`),check('todo_status_check',sql`${t.status} in ('open','completed')`),check('todo_title_check',sql`char_length(btrim(${t.title})) between 1 and 160`),check('todo_description_check',sql`${t.description} is null or char_length(${t.description}) <= 2000`),index('todo_organization_creator_context_created_idx').on(t.organizationId,t.creatorMembershipId,t.contextMembershipId,t.createdAt),index('todo_organization_context_creator_created_idx').on(t.organizationId,t.contextMembershipId,t.creatorMembershipId,t.createdAt),index('todo_organization_assignee_status_idx').on(t.organizationId,t.assigneeMembershipId,t.status)]);
