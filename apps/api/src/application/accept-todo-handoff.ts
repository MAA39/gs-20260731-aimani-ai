import { ApiError } from '../errors/api-error'; import type { Clock } from './create-todo'; import type { TodoHandoffRepository } from '../domain/todo-handoff';
export class AcceptTodoHandoff { constructor(private readonly repo:TodoHandoffRepository,private readonly clock:Clock){} async execute(userId:string,input:{organizationId:string;handoffId:string}){const m=await this.repo.findActiveMembershipForUser(userId,input.organizationId);if(!m)throw new ApiError('forbidden','This organization is not available to this user.');const o=await this.repo.acceptTodoHandoff({...input,recipientMembershipId:m.membershipId,now:this.clock.now()});if(o.kind==='not_found')throw new ApiError('not_found','Todo Handoff not found.');if(o.kind==='forbidden')throw new ApiError('forbidden','Not allowed.');if(o.kind==='conflict')throw new ApiError('conflict',o.reason);return o;}}
