import {and,eq,or,desc} from 'drizzle-orm'; import {todo,membership,organization} from '@amidala/db/schema';
export class TodoRepository {constructor(private readonly db:any){} async findActiveMembershipForUser(userId:string,organizationId:string){const r=await this.db.select().from(membership).where(and(eq(membership.userId,userId),eq(membership.organizationId,organizationId),eq(membership.status,'active'))).limit(1);return r[0]??null;} async findMembership(id:string,organizationId:string){const r=await this.db.select().from(membership).where(and(eq(membership.id,id),eq(membership.organizationId,organizationId),eq(membership.status,'active'))).limit(1);return r[0]??null;} async create(i:any){const r=await this.db.insert(todo).values(i).returning();return this.map(r[0]);} async list(i:any){const rows=await this.db.select({t:todo,orgName:organization.name}).from(todo).innerJoin(organization,eq(organization.id,todo.organizationId)).where(and(eq(todo.organizationId,i.organizationId),or(and(eq(todo.creatorMembershipId,i.currentMembershipId),eq(todo.contextMembershipId,i.contextMembershipId)),and(eq(todo.creatorMembershipId,i.contextMembershipId),eq(todo.contextMembershipId,i.currentMembershipId))))).orderBy(desc(todo.createdAt),desc(todo.id));const cur=await this.findMembership(i.currentMembershipId,i.organizationId),ctx=await this.findMembership(i.contextMembershipId,i.organizationId);return {organization:{organizationId:i.organizationId,name:rows[0]?.orgName??''},currentMember:{membershipId:cur.id,name:cur.displayName,title:cur.title},contextMember:{membershipId:ctx.id,name:ctx.displayName,title:ctx.title,relationshipKinds:[]},todos:rows.map((x:any)=>this.map(x.t))};} private map(r:any){return {todoId:r.id,organizationId:r.organizationId,contextMembershipId:r.contextMembershipId,title:r.title,description:r.description,status:r.status,creator:{membershipId:r.creatorMembershipId,name:'',title:null},assignee:{membershipId:r.assigneeMembershipId,name:'',title:null},createdAt:r.createdAt.toISOString(),updatedAt:r.updatedAt.toISOString()};}}
